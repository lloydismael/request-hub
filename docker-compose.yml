version: "3.9"

services:
  web:
    image: lloydismael12/request-hub:v50.6
    ports:
      - "127.0.0.1:8000:8000"
    environment:
      - APP_VERSION=v50.6
    # Host-only secrets. .env is gitignored and dockerignored; never copy it into the image.
    env_file:
      - .env
